@{
    ViewBag.Title = "SectionsUse";
}

<h2>SectionsUse</h2>

@section Header{    
    @foreach (string str in new string[] { "Home", "List", "Edit" })
    {
        <div style="float: left; padding: 5px;">
            @Html.ActionLink(str, str)
        </div>
    }
    <div style="clear: both"></div>
}
@section Footer{
    <h4>This is the footer</h4>
}
